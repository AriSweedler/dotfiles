" open files in new tabs
" 'gT' would notmally open a file in a backgound tab, but I have mapped over
" it for myself. So I cannot use it. I could make a new mapping to regain this
" capability, but... eh for now.
let g:netrw_browse_split = 3

" tree style listing
let g:netrw_liststyle = 3
