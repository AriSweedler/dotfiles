# Change the install location of tpm.
# Keep this in sync with 'TMUX_PLUGIN_MANAGER_PATH' in
#   ~/.config/zsh/plugins/tmux.zsh
set-environment -g TMUX_PLUGIN_MANAGER_PATH "~/.local/share/tmux/tpm_plugins"
