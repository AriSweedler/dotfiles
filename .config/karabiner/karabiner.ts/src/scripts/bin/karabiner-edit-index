#!/bin/bash

set -e

# Load in library commands
source "${REPO_LIB:?}/tmux_session_window.bash"

export TMUX_SESSION_WINDOW_SKIP_IF_APPLICATION="nvim"
cmd="${EDITOR:-nvim} '${REPO_ROOT:?}/src/index.ts'"
tmux_session_window::run_cmd "karabiner" "edit-index" "$cmd"
